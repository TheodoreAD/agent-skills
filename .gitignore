__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/
.env
site/
.cache/
# Where Claude Code's EnterWorktree puts a linked worktree. Untracked rather than ignored, it shows
# as `?? .claude/worktrees/` in every status and `git add -A` embeds it as a gitlink, exit 0.
.claude/worktrees/
